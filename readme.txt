1) All code is in index.js file and index.html file.
2) Don't need to install fs. fs used for file system like readfile, writefile etc.
3) Install chalk. Command is 'npm i chalk'. Chalk is used for color of console's message.
4) Install express. Command is 'npm i express'.
5) I used ajax to submit form data.
6) When click submit button, data is saved into entry.json file.